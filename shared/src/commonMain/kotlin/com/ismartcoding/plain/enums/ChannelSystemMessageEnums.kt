package com.ismartcoding.plain.enums

enum class ChannelSystemMessageType {
    INVITE,
    INVITE_ACCEPT,
    INVITE_DECLINE,
    UPDATE,
    KICK,
    LEAVE,
}

enum class ChannelSystemMessageAction {
    INVITE,
    UPDATE,
    KICK,
}