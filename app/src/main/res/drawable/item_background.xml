<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/control_highlight">
    <item android:id="@+id/mask">
        <color android:color="@color/canvas" />
    </item>
</ripple>