package com.ismartcoding.plain.data.enums

enum class PickFileType {
    IMAGE,
    IMAGE_VIDEO,
    FILE,
}

enum class PickFileTag {
    SEND_MESSAGE,
    SCAN,
    EDITOR,
    FEED,
    BOOK,
    RESTORE,
}
